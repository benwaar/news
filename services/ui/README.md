# News UI

Angular frontend for the News project. Placeholder folder.

- Generate the Angular app here (e.g., via Angular CLI) and configure the Dockerfile + Nginx as needed.
- TLS dev certs should live in `services/ui/certs` and be mounted by compose.
